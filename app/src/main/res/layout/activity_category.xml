<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.example.android.bookdroid.home.CategoryViewModel" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".home.CategoryActivity"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/downloadable_books"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:listData="@{viewModel.downloadableBooks}"
            tools:listitem="@layout/downloadable_book_item"/>

        <ImageView
            android:layout_width="180dp"
            android:layout_height="311dp"
            app:apiStatus="@{viewModel.downloadableBookStatus}" />

    </LinearLayout>

</layout>